import React, { FC } from 'react';
import { Container, Section } from '@components/common';

const Home: FC = () => (
  <Section>
    <Container>
      <h1>home</h1>
    </Container>
  </Section>
);

export default Home;
